<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
        <img src="/img/coffeelover.png" width="607" height="510" class="d-block mx-lg-auto ing-fluid" loading="lazy" alt="Coffee Lover">
    </div>

    <div class="col-12 col-lg-6">

        <h1 class="display-5 fw-bold mb-3">Discover Your New Favourite Coffee Recipe</h1>
            <p class="lead">
                Do you love coffee drinks, but not waiting in line to shell out big bucks at the coffee shop?
                 Be your own barista with these coffee recipes that let you make your favorite brewed drinks 
                 at home. Whether you crave a classic cappuccino, want to try your hand at flat whites, 
                 or are searching for the ultimate pumpkin spice latte copycat, you'll find what you need here.
            </p>

        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>
            <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Show Random</a>
        </div>


    </div>

</div>

<!-- Categories -->
<div class="row row-cols-2 row-cols-lg-6 g2 g-lg-3 py-4">
    
    <% if(categories != '') { %>
        <% categories.forEach(function(category, index) { %>
          <a href="/categories/<%- category.name %>" class="col text-center category_link">
            <div class="category_img shadow">
              <img src="img/<%- category.image %>" alt="<%- category.name %>" loading="lazy">
            </div>
            <div class="pt-1"><%- category.name %></div>
          </a>
        <% }) %>
      <% } %>

      <a href="/categories" class="col text-center category_link">
        <div class="category_img shadow">
          <img src="img/lmore.png" alt="View All Categories" loading="lazy">
        </div>
        <div class="pt-1">View All</div>
      </a>

</div>
<!-- Categories End -->
<br>

<!-- Latest -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

    <% if(coffee != '') { %>
        <% coffee.latest.forEach(function(recipe, index){ %>
        <a href="/recipe/<%= recipe._id %>" class="col text-center category_link">
          <div class="category_img category_img--large shadow">
            <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
          </div>
          <div class="pt-1"><%= recipe.name %></div>
        </a>
        <% }) %>
      <% } else { %>
        <p>No items found.</p>
      <% } %>
        
    </div>
<!-- Latest End-->

</section>


<!-- Each Category -->

<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Espresso Recipes</h2>
    <a href="/categories/Espresso" class="ms-auto">View More</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(typeof coffee.espresso !== 'undefined' && coffee.espresso.length > 0) { %>
      <% coffee.espresso.forEach(function(recipe, index){ %>
      <a href="/recipe/<%= recipe._id %>" class="col text-center category_link">
        <div class="category_img category_img--large shadow">
          <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
        </div>
        <div class="pt-1"><%= recipe.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>
  </div>
</section>

<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Cappuccino Recipes</h2>
    <a href="/categories/Cappuccino" class="ms-auto">View More</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(typeof coffee.cappuccino !== 'undefined' && coffee.cappuccino.length > 0) { %>
      <% coffee.cappuccino.forEach(function(recipe, index){ %>
      <a href="/recipe/<%= recipe._id %>" class="col text-center category_link">
        <div class="category_img category_img--large shadow">
          <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
        </div>
        <div class="pt-1"><%= recipe.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>
  </div>
</section>

<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Americano Recipes</h2>
    <a href="/categories/Americano" class="ms-auto">View More</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(typeof coffee.americano !== 'undefined' && coffee.americano.length > 0) { %>
      <% coffee.americano.forEach(function(recipe, index){ %>
      <a href="/recipe/<%= recipe._id %>" class="col text-center category_link">
        <div class="category_img category_img--large shadow">
          <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
        </div>
        <div class="pt-1"><%= recipe.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>
  </div>
</section>


<!-- Each Category End -->


<!-- Submit -->
<section class="px-4 py-5 my-5 text-center">
    <img src="/img/brewer.png" class="d-block mx-auto mb-4 img-fluid" alt="Publish Your Recipe" width="566" height="208" loading="lazy">
    <h1 class="display-5 fw-bold">Publish Your Coffee Recipe here today!</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">
            Publish your coffee Recipe to the world here for free.
        </p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="/submit-recipe" class="btn btn-primary btn-dark btn-lg">Submit Recipe</a>
        </div>
    </div>

</section>
<!-- Submit End -->


